#!/usr/bin/node
const argument_vector = process.argv.slice(2);
const scrii = argument_vector.length > 0 ? (argument_vector.length === 1 ? "Argument found" : "Arguments found") : "no Argument";
console.log(scrii;)
